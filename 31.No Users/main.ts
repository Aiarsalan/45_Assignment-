import { log } from "console";

let users : string [] = ["Admin", "Eric", "Arsalan", "Rizwan", "Furqan"];
if (users.length === 0) {
console.log("The list of users is not empty.!")
} else {

users = [];
console.log("We need to find some users! " + users.length  );

}